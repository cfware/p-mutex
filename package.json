{
	"name": "@cfware/p-mutex",
	"version": "0.3.0",
	"description": "Promise based mutex",
	"main": "index.mjs",
	"scripts": {
		"release": "standard-version --sign",
		"pretest": "cfware-lint .",
		"tests-only": "nyc -s --no-check-coverage node --experimental-loader @istanbuljs/esm-loader-hook test.mjs",
		"test": "npm run -s tests-only",
		"posttest": "nyc report"
	},
	"engines": {
		"nodejs": ">=13.2.0"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/cfware/p-mutex.git"
	},
	"author": "Corey Farrell",
	"license": "MIT",
	"keywords": [
		"promise",
		"lock",
		"mutex",
		"async",
		"sequence"
	],
	"bugs": {
		"url": "https://github.com/cfware/p-mutex/issues"
	},
	"homepage": "https://github.com/cfware/p-mutex#readme",
	"devDependencies": {
		"@cfware/lint": "^1.0.0",
		"@cfware/nyc": "^0.6.0",
		"@istanbuljs/esm-loader-hook": "^0.1.0",
		"libtap": "^0.3.0",
		"nyc": "^15.0.0",
		"standard-version": "^8.0.0"
	}
}
